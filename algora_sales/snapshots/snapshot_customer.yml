snapshots:
  - name: snapshot_customer
    relation: source('transactional_data_output', 'customer')
    config:
      unique_key: customer_id
      strategy: check
      check_cols:
        - email
        - country
      dbt_valid_to_current: "to_date('9999-12-31')"
